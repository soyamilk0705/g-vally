package test.buy;

// 클래스 -> 데이터 타입 -> 내가 만드는 데이터 타입
// 데이터 타입에 따라서 연산자가 결정되기 때문에 중요
// 연산자 -> 사용자가 만들 수 없는데 클래스는 내가 만들기 때문에 연산자는 함수가 대신할 수 있다.

public class CU {
	
	public String getIcecream(int left) {
		// TODO Auto-generated method stub
		//	6. 점원이 매대에서 아이스크림("누가바")를 가져온다.
		String result = "누가바";	
		//	7. (5번에 대한 응답으로) (점원이 로봇에게) 아이스크림(누가바)를 준다.
		return result;
	}
}
